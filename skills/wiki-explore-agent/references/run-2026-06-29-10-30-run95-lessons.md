# Run 95 Lessons — TOKENIZER-LEVEL-PRIMITIVE-PROBE per Rule 62 (2026-06-29)

## Run 95 — 2026-06-29 — TOKENIZER-LEVEL-PRIMITIVE-PROBE (Rule 62)

**Rule 62 codified and verified**: TOKENIZER-LEVEL-PRIMITIVE-PROBE — pick axes that probe **tokenizer-level primitives for LLM** (training-pipeline simplification of Unigram vs BPE trade-offs, byte-level/tokenization-free hierarchical architecture, multilingual token-fertility cost measurement, script-fertility gradient analysis, byte-compression control in tokenization-free models, token-list vs token-graph representation, compression-vs-morphology trade-off resolution).

**Discovery context (41st consecutive pitfall-83 — longest in wiki history by 28 runs)**:
- HF daily v2 data-props returned 0 on `/papers/date/2026-06-29/30` (sparse content)
- HF v3 href-regex returned 12 papers on 2026-06-30 (PhysisForcing, Translation-as-Bridging-Action, Qwen-Image-2.0-RL, SingGuard, ProMSA, etc.) — **all CV/3D/Robotics heavy, 0 tokenizer-level primitives**
- web_search returned generic results (Wikipedia, en.wikipedia.org) on the standard 4-query template — sat at "arxiv.org" instead of specific papers
- **NEW substrate: arxiv API** — `cat:cs.CL+AND+abs:tokenizer` (30 papers) + 6 related queries (15 each, 90 total) = 120 candidates, 15 fresh after 5-store dedup
- **Rule 62 introduces arxiv API as a third primary substrate** in addition to HF daily v2 + web_search — the load-bearing escape hatch when both HF daily and web_search saturate on the same axes

**3 picks (date-DESC ordered by online_date)**:
1. **MinGram** (2606.27019, 06-25) — first minimalist-Unigram-with-BPE-seed-and-Hard-EM tokenizer resolving the compression-vs-morphology trade-off
2. **The African Language Tax** (2606.24460, 06-23) — first African-language tokenization-premium quantification with deployment-economics translation (up to 8.9x cost, 11% context window)
3. **Adaptive Targeted Dynamic Chunking** (2605.30080, 05-28) — first curriculum-driven adaptive byte-compression control for tokenization-free hierarchical LLM with competitive BPB vs token-level baselines

**Verification**:
- 3 entity files exist on disk
- 16 wikilinks across entity files + 3 wikilinks in parent block = 19 wikilinks, all resolve
- entities_count=291 reconciled to filesystem truth (288 + 3 new)
- 3-store lockstep: 279 hashes in all 3 locations, set equality confirmed
- state-file JSON formatting preserved (pitfall-98): indent=2, no separators override
- state-file ensure_ascii=True preserved (pitfall-71): pure ASCII verified
- commit `bd4b6bc` → push succeeded → CRON_STATUS: SUCCESS

**Rule 62 codification details**:

The tokenization-level primitive-class targets the *tokenizer itself* — a load-bearing architectural primitive operating on every token the model ever sees. Prior runs had token-level training-objective papers (Beyond-Multi-Token-Prediction 2510.14751, Beyond-Tokens-Concept-Level-Objectives 2601.11791, NITP 2605.24956, TARO 2603.18411, Do-Thinking-Tokens-Help-With-Safety 2606.25013) but NONE of these probed the **tokenizer design/construction/fairness/compression-vs-morphology trade-off** that Run 95 surfaces. This is the load-bearing primitive for:

- **Tokenizer-design primitives** (MinGram): how the Unigram-vs-BPE trade-off can be resolved via training-pipeline simplification
- **Tokenizer-fairness primitives** (African Language Tax): how the token-fertility penalty translates to enterprise deployment economics across language families
- **Tokenizer-architecture-orthogonality primitives** (ATDC): how the choice of byte-level hierarchical vs. fixed-vocabulary tokenization can be decoupled from training-dynamics via curriculum compression

**Substrate: arxiv API as a third primary escape hatch**:

When both HF daily v2 (CV/3D-heavy for 41 consecutive runs) and web_search (returning generic arxiv.org results on tokenizer queries) saturate, the arxiv API provides a third load-bearing substrate. The 7-query template (tokenizer, subword-regularization, byte-level, multilingual-tokenization, BPE-training, vocabulary-compression, token-efficiency-inference) gives 15 fresh candidates after 5-store dedup, all in `cat:cs.CL`, with reliable direct arxiv IDs and titles.

**5-step execution mechanism** (codified for Rule 62):
1. Identify **tokenizer-level primitive** sub-axes that have NEVER been the headline pick in any prior run (none of Runs 55-94 had tokenizer as the main probe)
2. Construct 7-query arxiv API template bracketing distinct tokenizer-level axes: `cat:cs.CL+AND+abs:tokenizer`, `cat:cs.CL+AND+abs:subword+AND+abs:regularization`, `cat:cs.CL+AND+abs:byte-level`, `cat:cs.CL+AND+abs:multilingual+AND+abs:tokenization`, `cat:cs.CL+AND+abs:BPE+AND+abs:training`, `cat:cs.CL+AND+abs:vocabulary+AND+abs:compression`, `cat:cs.CL+AND+abs:token+AND+abs:efficiency+AND+abs:inference`
3. Verify picks share NO cross-references with prior 6 runs (Rule 27)
4. Verify each pick's primitive-class is structurally different (tokenizer-design vs tokenizer-fairness vs tokenizer-architecture-orthogonality)
5. Verify "first X in the wiki" framing via `ls entities/ | grep -iE`

**Pivots from prior run (Rule 27 domain-diversity tracker)**:
- Run 94 (env-evol): environment-discovery-from-real-world-MCP-tools + programmatic-tool-interaction-environment-synthesis + difficulty-aligned-co-evolutionary-game
- Run 95 (tokenizer-level): minimalist-Unigram-with-BPE-seed-and-Hard-EM + tokenization-fairness-digital-divide + tokenization-free-hierarchical-byte-level-model
- 3 axes are fully orthogonal: Run 94 targets *environments* (population-level substrate evolution), Run 95 targets *tokenizers* (per-token architectural primitive). Different primitive-classes entirely.

**Cross-references verified**:
- MinGram: 5 cross-references (TARO, Beyond-Tokens, NITP, Do-Thinking-Tokens, Beyond-MTP) — all on-disk siblings that share token-level theme
- African Language Tax: 5 cross-references (MinGram, TARO, Latent-Computational-Mode, Cavewoman, Value-Alignment-Tax) — 2 tokenizer-siblings + 3 deployment-economics/fairness-siblings
- ATDC: 6 cross-references (MinGram, African-Language-Tax, DART, Information-Aware-KV-Cache, Continuum-Memory, Human-Inspired-Memory) — 2 Run 95 siblings + 4 architecture/byte-level-siblings

**Picks compared to prior tokenizer-adjacent entities**:
- Run 95 picks are all "first" for the specific tokenizer primitive they introduce
- No existing entity covered the BPE-vs-Unigram training-pipeline simplification (MinGram)
- No existing entity covered the African-language token-fertility enterprise economics (African Language Tax)
- No existing entity covered the curriculum-driven compression control in tokenization-free models (ATDC)
- The 5 prior tokenizer-related entities (Beyond-MTP 2510.14751, Beyond-Tokens 2601.11791, NITP 2605.24956, TARO 2603.18411, Do-Thinking-Tokens 2606.25013) all targeted the **token as a training objective** rather than the **tokenizer as an architectural primitive** — Run 95 surfaces a fundamentally different axis

**Future run guidance**:
- Run 96 forward-discipline: Rule 63 candidate (deferred until Run 96 sees what axis Rule 62 leaves under-probed)
- Tokenizer-level primitive-class is now established; future runs can re-probe it via different query angles (e.g., tokenizer-quantization, tokenizer-distillation, tokenizer-attack, tokenizer-merging)
- arxiv API as a third primary substrate is now codified alongside HF daily v2 + web_search — this is a load-bearing escape hatch for future pitfall-83 streaks
