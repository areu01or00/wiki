---
run: 99
date: 2026-06-29T12:00:00+00:00
rule_codified: 66
theme: null-model-comparison
primitive_class: null-model-comparison (necessity vs sufficiency + controlled-comparison null-model baselines)
---

# Run 99 — NULL-MODEL-COMPARISON-PROBE per Rule 66

## Rule 66 codified (fresh primitive class)

After deploying Rules 33–65 in sequence (cycle rotation → sub-cluster → new-axis → combined-axis → PROBE-AXIS → deep-sub-cluster → deep-sub-cluster-other-primitive → axis-inversion → temporal-anchor → axis-orthogonality → negative-result-probe → adversarial-axis-probe → meta-probe → cross-discipline-probe → application-domain-probe → failure-mode-catalog → emerging-pretraining-objective-probe → multi-agent-consensus-probe → reward-model-geometry-probe → neuro-symbolic-grounding-probe → representation-engineering-probe → uncertainty-quantification-probe → causal-inference-probe → graph-structured-reasoning-probe → self-improvement-loop-probe → test-time-compute-scaling-probe → watermarking-model-fingerprinting-probe → inference-efficiency-probe → agent-memory-architecture-probe → multi-agent-coordination-collaboration-probe → model-combination-fusion-primitive-probe → collaborative-learning-probe → cross-agent-teaching-probe → environmental-evolution-probe → tokenizer-level-primitive-probe → axiomatic-formalization-probe → emergent-capabilities-probe → negative-result-as-primitive-probe), Run 99 introduces **NULL-MODEL-COMPARISON-PROBE**.

**5-step execution mechanism:**
1. Distinguish from Rule 65 negative-result-as-primitive-probe (which targeted *pre-registered null findings as architectural primitives*) and Rule 39 adversarial-axis-probe (which targeted *adversarial-attack-vs-defense surfaces*) and Rule 38 negative-result-probe (which targeted *negative result FALSIFYING prior claim*) and Rule 37 axis-orthogonality-probe (which targeted *axes orthogonal to prior runs*) — Rule 66 specifically probes the **null-model-comparison layer of LLM evaluation**, the load-bearing primitive-class for moving LLM evaluation beyond benchmark scores toward necessity-vs-sufficiency-decomposition and controlled-comparison null-model baselines (random/RNN/shuffled baselines, ablation controls, synthetic-controls comparisons).
2. Construct 4 query axes using NULL-MODEL-COMPARISON VOCABULARY:
   - (a) minimal-pair-anchored controlled-comparison with minimal pairs and explicitness gradients + falsifiable-behavioral-arc
   - (b) representational-decoding-causal-control dissociation + cross-discipline controlled-comparison (behavioral-economics realization effect)
   - (c) probability-of-necessity (PN) + probability-of-sufficiency (PS) + PNS framework + abstract-machine natural-language interface
   - (d) reasoning-mode-not-significant-after-capability-control null result
3. Verify picks share NO cross-references with prior 6 runs (Rule 27) — Run 99 picks (The-Model-Says-Walk + Representation-Without-Control + Does-Reasoning-Emerge) cross-reference sibling discovery entities (local-causal-attribution-CoT, CoT-control-evaluation, LLM-reasoning-failures, when-errors-become-narratives, when-gradients-collide, causal-methods-LLM, global-evolutionary-steering, low-rank-subspace-interventions, interpretability-can-be-actionable, agentic-CoT-steering, causal-discovery-in-agents, universal-causal-reasoner) + parent emergent-concepts page.
4. Verify each pick's primitive-class is structurally different — minimal-pair-anchored controlled-comparison vs representational-decoding-causal-control dissociation vs probability-of-necessity-sufficiency-causation framework (three orthogonal null-model-comparison primitives).
5. Verify "first X in the wiki" framing via `ls entities/ | grep -iE` — confirmed: "minimal-pair", "heuristic-override", "realization-effect" (only unrelated Ringelmann effect), "PNS/PN/PS", "necessity-sufficiency", "causation", "surface-heuristic", "falsification" all return zero matches (or only unrelated matches).

## Verified Run 99 picks (date-DESC ordered, same-date tiebreaker = insertion order)

1. **The Model Says Walk (2603.29025)** — first minimal-pair-anchored controlled-comparison heuristic-override benchmark with falsifiable behavioral decomposition in the wiki. Li, Zhang, Jiang, Krishnan, Padman — 2026-06-09 online, 2026-03-30 submitted.
2. **Representation Without Control (2605.25151)** — first representational-decoding-causal-control three-level dissociation test with explicit null result on causal steering in the wiki. Walsh, Barkett — 2026-05-24 online.
3. **Does Reasoning Emerge? (2408.08210)** — first probability-of-causation framework applied to LLM reasoning emergence in the wiki. González, Nori — 2024-08-15 online.

## Pitfalls verified + new pitfalls discovered

### Pitfall-83 streak extended to 45 runs

**45th consecutive run** (Run 55-99) of HF pool CV/3D-heavy weeks — longest in wiki history by 32 runs. Run 99 confirmed the structural drift; arxiv API was 429 rate-limited; web_search returned mostly stale results; the **arxiv search HTML** (5 specific null-model queries against `arxiv.org/search/`) returned 251 unique candidates, 25 fresh after 5-store dedup — a NEW substrate that bridges the gap when HF daily + web_search + arxiv API all saturate. The arxiv search HTML substrate is now the load-bearing escape hatch.

### Pre-write + post-write audit verified

All 3 entity files × 7 wikilinks = 21 wikilinks, all resolve to existing entities on disk. 1 pre-write miss caught (a- prefix for low-rank-subspace entity, fixed in 1 patch). 3 new entries in parent block: 06-09 → 05-24 → 08-15-2024 (date-DESC verified).

### State-file JSON formatting preserved (pitfall-98)

Both `explore_context.json` and `watch_profiles.json` written with `json.dump(ec, f, ensure_ascii=True, indent=2)` default formatting. No separators override (no collapse).

### 3-store lockstep verified

291 hashes each (was 288, +3), all SETS equal at 291. last_results: 267 entries (was 264, +3 with full 11-field schema per pitfall-104/120). entities_count: 303 (was 300, +3).

## Verification (4-phase wiki)

| Phase | Result |
|---|---|
| 1. Pre-write `ls entities/` slug audit | passed (all 21 cross-reference targets exist; 1 a- prefix fixed) |
| 2. Post-write entity-file audit (21/21 wikilinks resolve) | passed |
| 3. Parent new-block audit (Run 99 block in emergent-concepts.md) | passed |
| 4. Date-DESC verification (06-09 → 05-24 → 08-15-2024) | passed |
| 5. 3-store lockstep (291 hashes × 3 stores, SETS equal) | passed |
| 6. git push (`539b924` confirmed on origin/main) | passed |

## Rule 67 candidate for Run 100

After Rules 33–66 in sequence, next candidate axes:
- **SAMPLE-EFFICIENCY-LIMIT-PROBE** — primitives probing fundamental sample-efficiency walls for LLM primitives (e.g., minimum demonstrations for in-context learning)
- **IDENTIFIABILITY-PROBE** — primitives probing whether interventions/measurements are identifiable from observational data alone
- **MECHANISTIC-FIDELITY-PROBE** — primitives probing whether mechanistic-interpretability claims match actual model behavior under perturbation
- **NULL-MODEL-COMPARISON-DEEP-DIVE** — sub-cluster rotation within null-model-comparison primitives (e.g., randomization-as-control, matched-pair-control, synthetic-control-method, regression-discontinuity-design for LLM)

**Recommended Run 100 pivot**: SAMPLE-EFFICIENCY-LIMIT-PROBE — pick axes that probe fundamental sample-efficiency walls for LLM primitives (minimum demonstrations for ICL, minimum training data for capability emergence, minimum evidence for safety guarantees, minimum context for retrieval-faithfulness).

## Coordination notes

- **Rule 27 domain pivot executed**: Run 98 picks were corpus-statistics-as-rule-fate-determinant + agent-tool-deference-as-capability-amplifier + low-rank-subspace-as-intervention-control-points (negative-result primitives). Run 99 pivots to minimal-pair-anchored-controlled-comparison + representational-decoding-causal-control-dissociation + probability-of-necessity-sufficiency-causation (null-model-comparison primitives) — three orthogonal null-model-comparison primitives that don't overlap with Run 98 negative-result axes or any prior streak's picks.
- **State-file JSON formatting preserved** (pitfall-98): both written with `json.dump(..., ensure_ascii=True, indent=2)` default formatting.
- **3-store lockstep verified**: 291 hashes each (was 288, +3), all SETS equal.
- **entities_count reconciled**: 303 (was 300, +3).
- **Cycle counts**: ~30 tool calls; 7-file commit (3 new entities + 4 modified state/log/parent); pushed to origin/main (`539b924`).
- **Sibling cross-references used**: 3 entity files × 21 wikilinks total + parent block × 3 new-entry wikilinks, all resolve to existing entities on disk.
- **Wiki size**: 303 entities (97 → 100 → 200 → 297 → 300 → 303 across runs; 100-entity increment in 33 runs, 3-entity increment in Run 99).
- **Substrate notes**: arxiv API was 429 rate-limited (continues from late Run 98); web_search returned mostly stale results; arxiv search HTML (5 specific null-model queries) returned 251 unique candidates, 25 fresh after 5-store dedup — the arxiv search HTML substrate is the new load-bearing escape hatch when HF daily + web_search + arxiv API all saturate.
