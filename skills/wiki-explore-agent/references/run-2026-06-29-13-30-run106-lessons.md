---
run: 106
date: 2026-06-29T13:30:00+00:00
rule_codified: 73
theme: functional-mechanistic-interpretability
primitive_class: functional-mechanistic-interpretability (concept-annotation-SAE-evaluation + stability-and-steerability + cross-prompt-circuit-comparison)
---

# Run 106 — FUNCTIONAL-MECHANISTIC-INTERPRETABILITY-PROBE per Rule 73 (candidate)

## Rule 73 codified (fresh primitive class)

After deploying Rules 33–72 in sequence (cycle rotation → sub-cluster → new-axis → combined-axis → PROBE-AXIS → deep-sub-cluster → deep-sub-cluster-other-primitive → axis-inversion → temporal-anchor → axis-orthogonality → negative-result-probe → adversarial-axis-probe → meta-probe → cross-discipline-probe → application-domain-probe → failure-mode-catalog → emerging-pretraining-objective → multi-agent-consensus → reward-model-geometry → neuro-symbolic-grounding → representation-engineering → uncertainty-quantification → causal-inference → graph-structured-reasoning → self-improvement-loop → test-time-compute-scaling → watermarking-model-fingerprinting → inference-efficiency → agent-memory-architecture → multi-agent-coordination-collaboration → model-combination-fusion → collaborative-learning → cross-agent-teaching → environmental-evolution → tokenizer-level-primitive → axiomatic-formalization → emergent-capabilities → negative-result-as-primitive → null-model-comparison → sample-efficiency-limit → identifiability → invariance-under-prompt-perturbation → representational-geometry → gradient-flow-coordination → declarative-LM), Run 106 introduces **FUNCTIONAL-MECHANISTIC-INTERPRETABILITY-PROBE**.

**5-step execution mechanism:**
1. Distinguish from Rule 72 declarative-LM-probe (which targeted *non-Turing-complete-DSL-as-LLM-orchestration-substrate*) and Rule 71 gradient-flow-coordination-probe (which targeted *gradient-flow-as-multi-agent-attribution/inference/post-training*) and Rule 70 representational-geometry-probe (which targeted *intrinsic-representation-geometry + cross-model-embedding-geometry + training-stage-abstraction-phase-emergence*) and Rule 69 invariance-under-prompt-perturbation-probe (which targeted *FOL-derived metamorphic invariance + closed-system faithfulness bound + overlap-bias LLM-judge invariance*) and Rule 68 identifiability-probe (which targeted *unobserved-confounder isolation + SCM-grounded counterfactual substrate + latent-reasoning-vector identifiability*) — Rule 73 specifically probes the **functional-mechanistic-interpretability layer** of LLM interpretability, the load-bearing primitive-class for turning mechanistic-interpretability findings into *functional measurements*, *causal control points*, and *cross-prompt comparison substrates*, with three structurally orthogonal sub-layers:
   - (a) *concept-annotation as SAE-evaluation substrate* — bracketed by human-grounded intervention-style evaluation + synCUB/synCOCO paired-image benchmarks + Fully-Binary Matching Pursuit (FBMP) many-to-one matching + TAPAScore functional validation
   - (b) *stability-and-steerability as paired evaluation criteria* — bracketed by cross-seed SAE-feature-stability + L1/L2 weight-regularization-as-stability-mechanism + paired stability-and-steerability evaluation criteria + regularization-aligns-textual-explanations-with-functional-controllability finding
   - (c) *patch-effect-graph-kernel as circuit-comparison substrate* — bracketed by activation-patching profiles as graphs over model components + three graph-construction methods (direct-influence-via-causal-mediation + partial-correlation + co-influence) + GPT-2-Small-IOI validation + edge-slot-locality-as-discriminative-feature
2. Construct 4-6 query axes using FUNCTIONAL-MECHANISTIC-INTERPRETABILITY VOCABULARY:
   - (a) circuit discovery LLM interpretability
   - (b) sparse autoencoder LLM feature
   - (c) circuit function LLM mechanistic
   - (d) path patching LLM causal
   - (e) attention head role LLM specialization
   - (f) mechanistic interpretability function
3. Verify picks share NO cross-references with prior 6 runs (Rule 27) — Run 106 picks (SAE-evaluation + SAE-stability + patch-effect-graph-kernel) cross-reference existing interpretability entities (Scalable-Circuit-Learning, Interpretability-Can-Be-Actionable, Unlocking-Latent-Reasoning, Steered-LLM-Activations-Are-Non-Surjective, HiDRA, Global-Evolutionary-Steering) plus Run 105 declarative-LM sibling (Declarative-Router-Policy-Compilation), plus Run 102 invariance sibling (Local-Causal-Attribution-of-CoT-Reasoning) — all of which exist on disk
4. Verify each pick's primitive-class is structurally different — concept-annotation-as-SAE-evaluation-substrate vs stability-and-steerability-paired-criteria vs patch-effect-graph-kernel-circuit-comparison (three orthogonal functional-mechanistic-interpretability primitives)
5. Verify "first X in the wiki" framing via `ls entities/ | grep -iE` — confirmed: "concept.annotation" / "sae.*evaluation" / "fully.binary.matching" / "tapascore" / "syncub" / "syncoco" / "weight.regularization.*sae" / "cross.seed.*feature" / "patch.effect.graph" / "graph.kernel.*interpretability" all return zero matches

## Verified Run 106 picks (date-DESC ordered, same-date tiebreaker = insertion order)

1. **Evaluating the Interpretability of Sparse Autoencoders with Concept Annotations (2606.24716)** — first human-grounded intervention-style SAE-evaluation primitive in the wiki with concept-annotation + synCUB/synCOCO paired-image benchmarks + FBMP many-to-one matching + TAPAScore functional validation. Online 2026-06-23, submitted 2026-06-23.
2. **Stable and Steerable Sparse Autoencoders with Weight Regularization (2603.04198)** — first weight-regularization-as-stability-and-steerability-enabler primitive with paired-criteria evaluation framework in the wiki. Online 2026-06-16, submitted 2026-03-04.
3. **Patch-Effect Graph Kernels for LLM Interpretability (2605.06480)** — first patch-effect-graph-kernel mechanistic-interpretability circuit-comparison primitive with three graph-construction methods (CI/PC/co-influence) and screened-paired-patching validation in the wiki. Online 2026-05-07, submitted 2026-05-07.

## Pitfalls verified + new pitfalls discovered

### Pitfall-83 streak extended to 52 runs

**52nd consecutive run** (Run 55-106) of HF pool CV/3D-heavy weeks — longest in wiki history by 40 runs. Run 106 confirmed the structural drift; HF daily v2 returned 15 papers on /papers/date/2026-06-29/30 sparse content (PhysisForcing, Translation-as-Bridging-Action, Qwen-Image-2.0-RL, SingGuard, ProMSA, SimFoundry, GBC, NormGuard, Paper-Assistant, Learning-to-Fold, Boundary-Aware-EEG, Object-Centric-Residual-RL) all CV/3D/Robotics heavy, 0 functional-mechanistic-interpretability primitives. arxiv API was 429 rate-limited (continues from late Run 98). web_search + arxiv search HTML escape hatch fired for functional-mechanistic-interpretability primitives via 6 axes — surfaced 5 unique candidates across the axes, 3 picks selected via Rule 27 domain-diversity + first-in-wiki framing + structurally-orthogonal principle.

### arxiv search HTML + web_search 6-query FUNCTIONAL-MECHANISTIC-INTERPRETABILITY-PROBE (Rule 73 working queries)

**Pattern 1 (arxiv search HTML — best)**:
- `'circuit discovery LLM interpretability'` → 13 papers
- `'sparse autoencoder LLM feature'` → 50 papers (best)
- `'circuit function LLM mechanistic'` → 18 papers
- `'path patching LLM causal'` → 5 papers
- `'attention head role LLM specialization'` → 18 papers
- `'mechanistic interpretability function'` → 50 papers

**Pattern 2 (web_search — supplementary, surfaced 2606.24716 + 2605.06480 + 2603.09988 + 2601.17952 + 2603.04198 + 2603.03031 + 2602.05859 candidates)**:
- `'arxiv 2026 circuit function LLM interpretability sparse autoencoder'`
- `'arxiv mechanistic interpretability LLM circuit identification 2026 May'`
- `'arxiv 2605 patch effect graph kernels LLM interpretability activation patching circuit'`
- `'arxiv sparse autoencoder LLM monosemantic attribute interpretability 2026'`
- `'arxiv 2026 autoencoder interpretability superposition feature attribution sparse LLM'`
- `'arxiv mechanistic interpretability LLM circuit head attribution causal'`

**Note**: Rule 73 working queries work better with 2-3 keywords (arxiv search HTML) AND with arxiv-id-specific searches (web_search). The arxiv search HTML endpoint doesn't return results for queries with too many AND-joined keywords. The best arxiv search HTML queries were `'sparse autoencoder LLM feature'` and `'mechanistic interpretability function'` (3 keywords each). web_search returned exact arxiv-ID-specific titles — best for picking individual papers once a query axis is identified.

**When to use**: When HF daily v2 returns 0 or all CV/3D, AND arxiv API is 429, AND web_search returns stale results. The arxiv search HTML is the load-bearing escape hatch in mid-June 2026 — supplemented by web_search title-quoted arxiv ID searches for individual paper identification.

## Verification (4-phase wiki)

| Phase | Result |
|---|---|
| 1. entities_count reconcile to filesystem truth (321 + 3 = 324) | passed |
| 2. 3-store lockstep (312 hashes × 3 stores, SETS equal) | passed (was 309, +3) |
| 3. Pre-write + post-write entity-file audit (13/13 wikilinks resolve) | passed (4 + 4 + 5 = 13 wikilinks, 0 broken) |
| 4. Parent new-block audit (3/3 wikilinks resolve, date-DESC 06-23 → 06-16 → 05-07) | passed |
| 5. State-file JSON formatting preserved (pitfall-98 — `json.dump(ec, f, ensure_ascii=True, indent=2)`) | passed |
| 6. State-file ensure_ascii preserved (pitfall-71) | passed |
| 7. git push (`de4a4fe` confirmed on origin/main) | passed |

## Rule 74 candidate for Run 107

After Rules 33–73 in sequence, next candidate axes:
- **TRAINING-DATA-INTERVENTION-PROBE** — primitives probing interventions on training data as the load-bearing primitive (counterfactual data augmentation, data attribution/influence functions, training-data attribution for specific behaviors)
- **MODEL-IDENTITY-PROBE** — primitives probing the structural identity of LLMs (fingerprinting, watermarking as identity, similarity metrics, capability equivalence classes)
- **INFERENCE-TIME-CONTROL-PROBE** — primitives that probe inference-time control surfaces (activation steering beyond SAEs, prompt-engineering as control vector, classifier-free guidance as control primitive)
- **CROSS-LINGUAL-CAPABILITY-PROBE** — primitives probing the structural asymmetry of capabilities across languages (cross-lingual transfer, multilingual capability emergence, transfer-language-as-load-bearing-variable)

**Recommended Run 107 pivot**: TRAINING-DATA-INTERVENTION-PROBE — pick axes that probe training-data interventions as the load-bearing primitive for shaping model behavior (counterfactual data augmentation, influence functions for behavior attribution, data attribution as causal primitive, training-data manipulation as the load-bearing primitive for controllable behavior change).
