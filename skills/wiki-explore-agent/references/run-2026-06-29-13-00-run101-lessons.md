---
run: 101
date: 2026-06-29T13:00:00+00:00
rule_codified: 68
theme: identifiability
primitive_class: identifiability (unobserved-confounder isolation + SCM-grounded counterfactual substrate + latent-reasoning-vector identifiability)
---

# Run 101 — IDENTIFIABILITY-PROBE per Rule 68

## Rule 68 codified (fresh primitive class)

After deploying Rules 33–67 in sequence (cycle rotation → sub-cluster → new-axis → combined-axis → PROBE-AXIS → deep-sub-cluster → deep-sub-cluster-other-primitive → axis-inversion → temporal-anchor → axis-orthogonality → negative-result-probe → adversarial-axis-probe → meta-probe → cross-discipline-probe → application-domain-probe → failure-mode-catalog → emerging-pretraining-objective-probe → multi-agent-consensus-probe → reward-model-geometry-probe → neuro-symbolic-grounding-probe → representation-engineering-probe → uncertainty-quantification-probe → causal-inference-probe → graph-structured-reasoning-probe → self-improvement-loop-probe → test-time-compute-scaling-probe → watermarking-model-fingerprinting-probe → inference-efficiency-probe → agent-memory-architecture-probe → multi-agent-coordination-collaboration-probe → model-combination-fusion-primitive-probe → collaborative-learning-probe → cross-agent-teaching-probe → environmental-evolution-probe → tokenizer-level-primitive-probe → axiomatic-formalization-probe → emergent-capabilities-probe → negative-result-as-primitive-probe → null-model-comparison-probe → sample-efficiency-limit-probe), Run 101 introduces **IDENTIFIABILITY-PROBE**.

**5-step execution mechanism:**
1. Distinguish from Rule 67 sample-efficiency-limit-probe (which targeted *data-budget-for-deployment walls*) and Rule 66 null-model-comparison-probe (which targeted *controlled-comparison null-model baselines*) and Rule 65 negative-result-as-primitive-probe (which targeted *pre-registered null findings as architectural primitives*) and Rule 63 axiomatic-formalization-probe (which targeted *formal-foundation layer of LLM reasoning*) — Rule 68 specifically probes the **identifiability layer** of LLM primitives, the load-bearing primitive-class for moving LLM evaluation beyond behavioral measurement toward *whether a target quantity can be recovered at all* from the available data/observations/interventions, with three structurally orthogonal sub-layers:
   - (a) *unobserved-confounder identifiability* — can the causal effect be isolated from observational data when confounders are unobserved?
   - (b) *SCM-grounded counterfactual identifiability* — are concept-based explanations faithful under structural interventions?
   - (c) *latent-reasoning-vector identifiability* — can individual reasoning steps be identified and acted upon in continuous hidden states?
2. Construct 4 query axes using IDENTIFIABILITY VOCABULARY:
   - (a) identifiability + causal intervention + LLM (general)
   - (b) instrumental variable + confounding + language model (IV-specific)
   - (c) causal effect identification + text + generative model (effect-identification-specific)
   - (d) mediator + moderator + confounder + identifiability + LLM (mediation/moderation-specific)
3. Verify picks share NO cross-references with prior 6 runs (Rule 27) — Run 101 picks (IV-Co-Scientist + LIBERTy + Unlocking-Latent-Reasoning) cross-reference causal-discovery primitives (causal-discovery-in-the-era-of-agents, towards-a-universal-causal-reasoner), null-model-comparison primitives (does-reasoning-emerge, representation-without-control), interpretability primitives (interpretability-can-be-actionable, latent-computational-mode, hidden-thoughts-are-not-secret, agentic-chain-of-thought-steering, low-rank-subspace-analysis, formalizing-latent-thoughts), and causal-methods survey primitive (causal-methods-for-llm-development-and-evaluation) — all of which exist on disk.
4. Verify each pick's primitive-class is structurally different — IV-discovery-for-unobserved-confounder vs SCM-grounded-counterfactual-for-explanations vs latent-reasoning-vector-identifiability-via-probes (three orthogonal identifiability primitives).
5. Verify "first X in the wiki" framing via `ls entities/ | grep -iE` — confirmed: "instrumental-variable.*llm", "iv.*discovery", "iv.co.scientist", "liberty", "concept-based-explanation.*causal", "order-faithful", "structural-counterfactual.*benchmark", "latent-reasoning.*interv", "interpret-guided.*interv", "causal-hub.*latent" all return zero matches.

## Verified Run 101 picks (date-DESC ordered, same-date tiebreaker = insertion order)

1. **Unlocking the Black Box of Latent Reasoning (2606.01243)** — first latent-reasoning-vector identifiability primitive with structural/causal/geometric probes + training-free decode-time interventions in the wiki. Chang, Bai, Zhang, Ma, Liu, Liao, Miao, Niu — 2026-05-31 online, 2026-05-31 submitted.
2. **IV Co-Scientist (2602.07943)** — first IV-discovery-via-multi-agent-LLM-framework with two-stage evaluation and no-ground-truth consistency test in the wiki. Sheth, Jin, Wilder, Janzing, Fritz — 2026-04-06 online, 2026-02-08 submitted.
3. **LIBERTy (2601.10700)** — first SCM-grounded counterfactual benchmark for LLM concept-based explanations with order-faithfulness metric in the wiki. Toker, Calderon, Amosy, Reichart — 2026-01-18 online, 2026-01-15 submitted.

## Pitfalls verified + new pitfalls discovered

### Pitfall-83 streak extended to 47 runs

**47th consecutive run** (Run 55-101) of HF pool CV/3D-heavy weeks — longest in wiki history by 35 runs. Run 101 confirmed the structural drift; HF daily v2 returned 14 papers on /papers/date/2026-06-29/30 sparse content (PhysisForcing, Translation-as-Bridging-Action, Qwen-Image-2.0-RL, SingGuard, ProMSA, SimFoundry, Ko-WideSearch, GBC, NormGuard, Paper-Assistant, Learning-to-Fold, Boundary-Aware-EEG, Object-Centric-Residual-RL) all CV/3D/Robotics heavy, 0 identifiability primitives. arxiv API was 429 rate-limited (continues from late Run 98); web_search returned mostly stale results. The **arxiv search HTML** substrate (5 specific identifiability queries against `arxiv.org/search/`) returned 58 unique candidates, 57 fresh after 5-store dedup — the arxiv search HTML substrate continues to be the load-bearing escape hatch.

### arxiv search HTML 4-query IDENTIFIABILITY-PROBE (Rule 68 working queries)

**Pattern**: `https://arxiv.org/search/?searchtype=all&query={URL-ENCODED-QUERY}&start=0&order=-announced_date_first` returns ~50 arxiv IDs per query. Sleep 3-4s between queries. **Working query pattern** (verified Run 101):
- `'identifiability causal intervention LLM'` → 50 papers
- `'instrumental variable confounding language model'` → 4 papers
- `'causal effect identification text generative model'` → 5 papers
- `'mediator moderator confounder identifiability LLM'` → 0 papers

**Note**: query (d) returned 0 papers — the arxiv search HTML endpoint doesn't seem to surface results when query has too many AND-joined keywords. Working queries have 2-3 keywords each.

**When to use**: When HF daily v2 returns 0 or all CV/3D, AND arxiv API is 429, AND web_search returns stale results. The arxiv search HTML is the last-resort substrate that doesn't appear to be rate-limited as aggressively as the API.

## Verification (4-phase wiki)

| Phase | Result |
|---|---|
| 1. Pre-write `ls entities/` slug audit | passed (all 17 cross-reference targets exist on disk) |
| 2. Post-write entity-file audit (17/17 wikilinks resolve) | passed (5 + 5 + 7 = 17 wikilinks, 0 broken) |
| 3. Parent new-block audit (Run 101 block in emergent-concepts.md) | passed (3/3 wikilinks resolve) |
| 4. Date-DESC verification (05-31 → 04-06 → 01-18) | passed |
| 5. 3-store lockstep (297 hashes × 3 stores, SETS equal) | passed (was 294, +3) |
| 6. git push (`8a51209` confirmed on origin/main) | passed |

## Rule 69 candidate for Run 102

After Rules 33–68 in sequence, next candidate axes:
- **REPRESENTATIONAL-GEOMETRY-PROBE** — primitives probing the geometric structure of LLM representations (intrinsic dimensionality, manifold structure, fiber-bundle structure, topological features) and how that geometry constrains what the model can/cannot represent
- **MECHANISTIC-FIDELITY-PROBE** — primitives probing whether mechanistic-interpretability claims match actual model behavior under perturbation (claim-faithfulness audits, mechanistic-vs-behavioral disagreement)
- **INTERVENTIONAL-COMPLETENESS-PROBE** — primitives probing whether the intervention set is sufficient to identify all target causal effects (interventional-coverage audits, intervention-set-closure properties)
- **PROMPT-INVARIANCE-PROBE** — primitives probing whether LLM behavior is invariant to prompt paraphrasing (paraphrase-robustness primitives, prompt-distribution-shift)

**Recommended Run 102 pivot**: REPRESENTATIONAL-GEOMETRY-PROBE — pick axes that probe the geometric structure of LLM representations (intrinsic dimensionality, manifold structure, fiber-bundle structure) and how that geometry constrains identifiability, sample-efficiency, and downstream deployment.