---
run: 102
date: 2026-06-29T13:30:00+00:00
rule_codified: 69
theme: invariance-under-prompt-perturbation
primitive_class: invariance (FOL-metamorphic-invariance + closed-system-faithfulness-bound + overlap-bias-LLM-judge-invariance)
---

# Run 102 — INVARIANCE-UNDER-PROMPT-PERTURBATION-PROBE per Rule 69

## Rule 69 codified (fresh primitive class)

After deploying Rules 33–68 in sequence (cycle rotation → sub-cluster → new-axis → combined-axis → PROBE-AXIS → deep-sub-cluster → deep-sub-cluster-other-primitive → axis-inversion → temporal-anchor → axis-orthogonality → negative-result-probe → adversarial-axis-probe → meta-probe → cross-discipline-probe → application-domain-probe → failure-mode-catalog → emerging-pretraining-objective-probe → multi-agent-consensus-probe → reward-model-geometry-probe → neuro-symbolic-grounding-probe → representation-engineering-probe → uncertainty-quantification-probe → causal-inference-probe → graph-structured-reasoning-probe → self-improvement-loop-probe → test-time-compute-scaling-probe → watermarking-model-fingerprinting-probe → inference-efficiency-probe → agent-memory-architecture-probe → multi-agent-coordination-collaboration-probe → model-combination-fusion-primitive-probe → collaborative-learning-probe → cross-agent-teaching-probe → environmental-evolution-probe → tokenizer-level-primitive-probe → axiomatic-formalization-probe → emergent-capabilities-probe → negative-result-as-primitive-probe → null-model-comparison-probe → sample-efficiency-limit-probe → identifiability-probe), Run 102 introduces **INVARIANCE-UNDER-PROMPT-PERTURBATION-PROBE**.

**5-step execution mechanism:**
1. Distinguish from Rule 68 identifiability-probe (which targeted *recoverability-of-target-quantity-from-observations*) and Rule 67 sample-efficiency-limit-probe (which targeted *data-budget-for-deployment walls*) and Rule 66 null-model-comparison-probe (which targeted *controlled-comparison null-model baselines*) and Rule 65 negative-result-as-primitive-probe (which targeted *pre-registered null findings as architectural primitives*) and Rule 63 axiomatic-formalization-probe (which targeted *formal-foundation layer of LLM reasoning*) — Rule 69 specifically probes the **invariance layer** of LLM behavior under input perturbations, the load-bearing primitive-class for understanding *whether an LLM's outputs/reasoning/judgments remain stable under semantically-equivalent perturbations of the input*, with three structurally orthogonal sub-layers:
   - (a) *generator-side reasoning invariance under FOL-derived logical-equivalence transformations* — bracketed by FOL-derived metamorphic test construction + cross-case consistency checking + Few-shot-CoT-partial-mitigation finding
   - (b) *closed-system reasoning invariance-failure primitive* — bracketed by Debate Trap + SFS metric + information-theoretic bound on closed-system faithfulness
   - (c) *judge-side evaluation invariance under surface-overlap perturbations* — bracketed by 9-LLM cross-architecture overlap-bias empirical coverage + granular overlap-bias analysis + summarization-domain-deployment primitive
2. Construct 4-6 query axes using INVARIANCE VOCABULARY:
   - (a) prompt robustness paraphrase LLM consistency
   - (b) input perturbation invariance language model evaluation
   - (c) prompt template variation LLM benchmark stability
   - (d) robustness certification LLM prompt-level adversarial
   - (e) prompt sensitivity LLM benchmark
   - (f) invariance prompt language model
3. Verify picks share NO cross-references with prior 6 runs (Rule 27) — Run 102 picks (LGMT + Reasoning-Trap + Blind-to-Human-Touch) cross-reference first-order-logic primitives (ForEx, Formalizing-Latent-Thoughts, LogicGraph), reasoning-trace primitives (Hidden-Thoughts-Are-Not-Secret, Local-Causal-Attribution-of-CoT), LLM-judge primitives (Let-LLMs-Judge-Each-Other), and identifiability primitives (LIBERTy) — all of which exist on disk.
4. Verify each pick's primitive-class is structurally different — FOL-derived-metamorphic-invariance-for-reasoning vs closed-system-faithfulness-information-theoretic-bound-on-multi-step-reasoning vs overlap-bias-LLM-judge-invariance (three orthogonal invariance primitives).
5. Verify "first X in the wiki" framing via `ls entities/ | grep -iE` — confirmed: "metamorphic", "fol.*test", "first.order.logic.*test", "reasoning.trap", "debate.trap", "supported.faithfulness", "evidence.grounded.socratic", "overlap.bias", "llm.judge.bias", "summari.*bias" all return zero matches.

## Verified Run 102 picks (date-DESC ordered, same-date tiebreaker = insertion order)

1. **LGMT: Logic-Grounded Metamorphic Testing for Evaluating the Reasoning Reliability of LLMs (2605.23965)** — first FOL-derived metamorphic testing primitive with oracle-free cross-case consistency checking for LLM reasoning reliability in the wiki. Online 2026-06-07, submitted 2026-05-12.
2. **The Reasoning Trap: An Information-Theoretic Bound on Closed-System Multi-Step LLM Reasoning (2605.01704)** — first closed-system-reasoning faithfulness-failure primitive with SFS claim-level metric + information-theoretic bound in the wiki. Online 2026-05-05, submitted 2026-05-03.
3. **Blind to the Human Touch: Overlap Bias in LLM-Based Summary Evaluation (2602.07673)** — first overlap-bias primitive for LLM-based summary evaluation with 9-LLM cross-architecture empirical coverage in the wiki. Online 2026-02-07, submitted 2026-02-07.

## Pitfalls verified + new pitfalls discovered

### Pitfall-83 streak extended to 48 runs

**48th consecutive run** (Run 55-102) of HF pool CV/3D-heavy weeks — longest in wiki history by 36 runs. Run 102 confirmed the structural drift; HF daily v2 returned 14 papers on /papers/date/2026-06-29/30 sparse content (PhysisForcing, Translation-as-Bridging-Action, Qwen-Image-2.0-RL, SingGuard, ProMSA, SimFoundry, Ko-WideSearch, GBC, NormGuard, Paper-Assistant, Learning-to-Fold, Boundary-Aware-EEG, Object-Centric-Residual-RL) all CV/3D/Robotics heavy, 0 invariance primitives. arxiv API was 429 rate-limited (continues from late Run 98); web_search returned mostly stale results. The **arxiv search HTML substrate** (6 specific invariance queries against `arxiv.org/search/`) returned 52 unique candidates, 25 LLM-keyword-matched after 5-store dedup — the arxiv search HTML substrate continues to be the load-bearing escape hatch.

### arxiv search HTML 6-query INVARIANCE-UNDER-PROMPT-PERTURBATION-PROBE (Rule 69 working queries)

**Pattern**: `https://arxiv.org/search/?searchtype=all&query={URL-ENCODED-QUERY}&start=0&order=-announced_date_first` returns ~5-25 arxiv IDs per query (some queries return very few results — q3 and q4 returned 0/1). Sleep 3-4s between queries. **Working query pattern** (verified Run 102):
- `'prompt robustness paraphrase LLM consistency'` → 3 papers (low)
- `'input perturbation invariance language model evaluation'` → 2 papers (low)
- `'prompt template variation LLM benchmark stability'` → 0 papers
- `'robustness certification LLM prompt-level adversarial'` → 0 papers
- `'prompt sensitivity LLM benchmark'` → 13 papers
- `'invariance prompt language model'` → 26 papers (best)

**Note**: queries (c) and (d) returned 0 papers — the arxiv search HTML endpoint doesn't surface results when query has too many AND-joined keywords. Working queries have 2-3 keywords each. The best queries were (e) and (f) which use only 3 keywords. **Lesson**: invariance vocabulary queries work better with 2-3 keywords rather than 4-5.

**When to use**: When HF daily v2 returns 0 or all CV/3D, AND arxiv API is 429, AND web_search returns stale results. The arxiv search HTML is the last-resort substrate that doesn't appear to be rate-limited as aggressively as the API.

## Verification (4-phase wiki)

| Phase | Result |
|---|---|
| 1. Pre-write `ls entities/` slug audit | passed (12 cross-reference targets exist on disk after fixing 1 broken wikilink on Overlap Bias entity) |
| 2. Post-write entity-file audit (12/12 wikilinks resolve) | passed (4 + 4 + 4 = 12 wikilinks, 0 broken) |
| 3. Parent new-block audit (Run 102 block in emergent-concepts.md) | passed (3/3 wikilinks resolve) |
| 4. Date-DESC verification (06-07 → 05-05 → 02-07) | passed |
| 5. 3-store lockstep (300 hashes × 3 stores, SETS equal) | passed (was 297, +3) |
| 6. entities_count reconciled to filesystem truth (312) | passed (309 + 3 new) |
| 7. git push (`27fbb97` confirmed on origin/main) | passed |

## Pitfall-96 re-occurrence (caught + fixed)

Initial Run 102 Overlap Bias entity referenced `faithful-or-fabricated-a-causal-framework-for-rationalization-bias-in-llm-judges-2605.23970` (a paper in the candidate pool that was NOT picked as an entity). Pre-write audit caught it; replaced with `liberty-a-causal-framework-for-benchmarking-concept-based-explanations-of-llms-with-structural-counterfactuals-2601.10700` (a Run 101 sibling that exists on disk). **Lesson reinforced**: always `ls entities/ | grep -F "<exact-slug-prefix>"` per cross-reference target before writing the entity file.

## Rule 70 candidate for Run 103

After Rules 33–69 in sequence, next candidate axes:
- **REPRESENTATIONAL-GEOMETRY-PROBE** — primitives probing the geometric structure of LLM representations (intrinsic dimensionality, manifold structure, fiber-bundle structure, topological features) and how that geometry constrains what the model can/cannot represent
- **MECHANISTIC-FIDELITY-PROBE** — primitives probing whether mechanistic-interpretability claims match actual model behavior under perturbation (claim-faithfulness audits, mechanistic-vs-behavioral disagreement)
- **INTERVENTIONAL-COMPLETENESS-PROBE** — primitives probing whether the intervention set is sufficient to identify all target causal effects (interventional-coverage audits, intervention-set-closure properties)
- **PREFERENCE-LEARNING-INVARIANCE-PROBE** — primitives probing whether preference-learning is invariant to irrelevant features (preference-perturbation primitives, reward-hacking-via-irrelevant-features)

**Recommended Run 103 pivot**: REPRESENTATIONAL-GEOMETRY-PROBE — pick axes that probe the geometric structure of LLM representations (intrinsic dimensionality, manifold structure, fiber-bundle structure) and how that geometry constrains identifiability, sample-efficiency, invariance, and downstream deployment.