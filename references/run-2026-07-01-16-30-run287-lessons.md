## Run 287 — 2026-07-01T16:30:00+00:00

### Theme: Embodied Agent Skill Memory / Human Routine Navigation / Self-Directed APR Debugging
### Picks: HUMEMBR (2606.30404) / AtlasVA (2605.17933) / DebugRepair (2604.19305)

---

## Key Events

### HF DNS Failure (confirmed 3rd time — DNS-level, not HTTP)
`papers.foai.org` returned `Name or service not known` (DNS-level failure) across all 3 dates tried. This is a **fourth distinct failure mode** (Run 272: HTTP DNS; Run 277: HTTP 429; Run 286/287: DNS resolution failure). Per Rule: escalate immediately to web_search.

### Discovery: 27 fresh LLM candidates from 249 arxiv HTML IDs
web_search 5 queries returned thin results (medical/legal were dominated by non-arxiv). Escalated to arxiv HTML search (5 queries, 50 IDs each) which returned 249 unique IDs. 27 fresh after 5-store dedup — typical base rate.

### 3-Store Lockstep: MAINTAINED (869 → 872 after 3 picks)
Before: 869/869/869. After: 872/872/872. No lockstep drift.

### Sister-Cron Pre-Commit Pattern (NEW variant — Run 287)
MRT sister-cron pre-committed 3 entity files at 15:19 UTC (before this run). Wiki-watch sister-cron committed state files at 15:34 UTC. The Explore cron at 16:30 UTC found working tree clean — all 3 entity files already on main (93b2845 + f8b9333). Pattern confirmed as stable: entity files from MRT pre-commit, state files from wiki-watch, working tree clean at Explore start.

### Wikilink Broken Links: 2x causalnav/omnivln in HUMEMBR
HUMEMBR entity referenced `[[causalnav-2601.01872]]` and `[[omnivln-2603.17351]]` — neither exists on disk. Both were removed and replaced with `[[holoagent-0-2606.23565]]` and `[[from-reaction-to-anticipation-2605.11951]]` (both verified existing). Root cause: arxiv search returned these IDs but they were never committed to the wiki — so they should not be used as cross-reference targets.

### Git add -f Required
`git add entities/*.md` silently no-opped because `git ls-files` showed the files as already tracked (MRT pre-committed them), but `git status` showed nothing to commit. Using `git add -f <explicit paths>` forced the intent. The `.gitignore` file only contains `temp/` — the silent no-op was due to git's intent resolution, not an ignore rule.

---

## Run 287 Lessons

1. **HF DNS failure is a stable failure mode**: Run 272 (HTTP), Run 277 (HTTP 429), Run 286 (DNS), Run 287 (DNS). When DNS fails, it's immediate and unrecoverable within the same run. Always escalate to web_search + arxiv HTML search immediately.

2. **Broken wikilinks from candidate papers that were never in the wiki**: arxiv search returns IDs that were never committed. Always verify the target exists on disk before writing wikilinks — don't assume that because you can fetch metadata for an ID, it exists in the wiki.

3. **git add -f when pre-committed**: When entity files are already tracked by a sister-cron's pre-commit, `git add` silently no-opps even with explicit paths. `git add -f` forces the intent.

4. **5-query arxiv HTML search as primary substrate**: When HF DNS fails, 5 queries × 50 IDs = 250 total IDs returned 27 fresh LLM candidates. This is sufficient to find 3 structurally orthogonal picks.

---

## Theme-Pivot Verification (Rule 27)
- Run 286 theme: LLM-as-Judge / VLA reasoning / Self-evolving UQ
- Run 287 theme: Embodied navigation / VLM skill memory / APR debugging
- Orthogonality: All 3 Run 287 axes (embodied robotics, VLM agent memory, code generation) are structurally distinct from Run 286's axes (evaluation methodology, inference efficiency, uncertainty quantification). No cross-references overlap. ✓

## Query Template: 5-Query Deep-Sub-Cluster Other Primitive Class
Axes: embodied robot navigation + medical diagnostic reasoning + multimodal video understanding + synthetic data generation + automated bug repair. All 5 are PROBE-AXIS or deep-sub-cluster axes not used in Runs 285-286. 27 fresh candidates confirmed.
