# Run 108 — INFORMATION-GEOMETRIC-PRIMITIVE-PROBE per Rule 75 (candidate) — 2026-06-29 11:53 UTC

**Run 108** — 54th consecutive pitfall-83 (longest in wiki history by 42 runs, Runs 55-108) — introduced INFORMATION-GEOMETRIC-PRIMITIVE-PROBE per **Rule 75 (candidate)**.

## Discovery
- HF daily v2 returned 0 papers on /papers/date/2026-06-29/30 sparse pages (54th consecutive CV/3D-heavy HF pool)
- arxiv API rate-limited 429 (continues from late Run 98)
- web_search 4-query INFORMATION-GEOMETRIC-PRIMITIVE-PROBE escape hatch fired with axes:
  1. Fisher Information Matrix LLM fine-tuning
  2. optimal transport language model embedding
  3. information geometry transformer attention
  4. KL divergence token distribution LLM
- Surfaced 15 unique LLM candidates, all 15 fresh after 5-store dedup (528 known hashes)

## 3 picks (date-DESC ordered by submission_date)
1. **2604.07569** — Learning is Forgetting: LLM Training As Lossy Compression (04-08 sub) — **first rate-distortion/IB-bound framing of LLM training with signal-vs-distortion decomposition**
2. **2603.04355** — Efficient Refusal Ablation in LLM through Optimal Transport (03-04 sub) — **first PCA-preconditioned closed-form OT refusal-ablation preserving distributional shape in residual channels**
3. **2601.06851** — A Brain-like Synergistic Core in LLMs Drives Behaviour and Learning (01-11 sub) — **first Williams–Beer PID redundancy/synergy decomposition of LLM activations across model families with middle-layer synergistic core**

## Rule 75 INFORMATION-GEOMETRIC-PRIMITIVE-PROBE codified
Pick axes that explicitly probe **information-geometric primitives** — primitives that quantify the structure of LLM representations and learning via Fisher-information-metric / Partial-Information-Decomposition / rate-distortion / optimal-transport / differential-geometric frameworks over LLM states and parameters.

**5-step execution mechanism:**
(a) Identify the **layers** of information-geometry: activation-level / training-level / alignment-level / mechanistic-interpretability-level
(b) Construct 4-8 query axes using INFORMATION-GEOMETRIC VOCABULARY (Fisher Information Matrix, optimal transport, information geometry, KL divergence, partial information decomposition, Williams-Beer, rate-distortion, information bottleneck, statistical manifold, Wasserstein, Sinkhorn, geodesic distance, neural tangent kernel)
(c) Verify picks share NO cross-references with prior 6 runs (Rule 27)
(d) Verify each pick's primitive-class is structurally different (activation-level vs training-level vs alignment-level)
(e) Verify "first X in the wiki" framing via `ls entities/ | grep -iE`

**Distinct from existing primitives:**
- **Loss-landscape-geometry primitives** (Early-Warning-Signals-of-Grokking, 2602.16967): load-bearing axis is *information-geometric-representation-and-learning-primitive*, not *loss-surface-curvature*
- **Mechanistic-interpretability primitives** (Latent-Computational-Mode, Local-Causal-Attribution): load-bearing axis is *information-theoretic-synergy-of-components*, not *learned-circuit-feature-attribution*
- **Representational-geometry primitives** (Run 103): load-bearing axis is *statistical-manifold-distance-on-representation-space*, not *intrinsic-dimensionality-of-embedding*
- **Safety/Refusal primitives** (Geometry-of-Refusal): load-bearing axis is *distribution-to-distribution-map*, not *linear-direction-removal*
- **Memory/Cognitive-load primitives** (Run 107 BAPO CoT-token-complexity): load-bearing axis is *information-geometric-distance*, not *token-budget-complexity-bound*
- **Neural-Tangent-Kernel primitives** (RoCoFT 2210.05643): load-bearing axis is *information-geometric-statistical-manifold*, not *gradient-descent-dynamics*

## Pitfalls encountered + recovery
- **Pitfall-96 (slug typo)**: 2 broken cross-reference slugs caught during pre-write audit:
  - `latent-computational-mode-in-large-language-models-2601.08058` (missing `a-` prefix; actual slug: `a-latent-computational-mode-in-large-language-models-2601.08058`) — fixed via 1 patch call in brain-like-synergistic-core entity
  - `efficiency-aware-evaluation-and-attack-agnostic-protect-2606.24970` (entity doesn't exist on disk) — replaced with `multibreak-a-scalable-and-diverse-multi-turn-jailbreak-benchmark-for-evaluating-llm-safety-2605.01687` (existing entity) via 1 patch call in learning-is-forgetting entity
  - **Lesson**: ALWAYS run `ls entities/ | grep -F` to get exact slug, NEVER reconstruct from paper-title semantics. Both broken-wikilinks caught before commit, no post-write audit failures.
- **State-file format**: Used `json.dump(ec, f, ensure_ascii=True, indent=2)` with default formatting — no `separators=` override (pitfall-98).
- **Lockstep verification**: 3-store hashes are LIST-OF-STRINGS (pitfall-120), verified by **set equality** `set(top) == set(llm_wiki) == set(explore) == True`. List equality is False (different insertion orders) — that's expected and correct.

## Verification
- All 3 entity files exist on disk ✓
- All 16 cross-reference wikilinks resolve to existing entities (6+5+5) ✓
- entities_count=330 reconciled to filesystem truth (327 prior + 3 new) ✓
- 3-store lockstep confirmed via **set equality** (318 hashes each, was 315, +3) ✓
- state-file JSON formatting preserved (pitfall-98) ✓
- state-file ensure_ascii=True preserved (pitfall-71) ✓
- pre-write + post-write audit verified (0 broken wikilinks - pitfall-21/31/96/107/109) ✓
- date-DESC ordering verified: 08 Apr 2026 → 04 Mar 2026 → 11 Jan 2026 (correct DESC by submission_date) ✓
- Verified commit + push (`51c77b6` confirmed via `git push origin main` ✓)

## Rule 76 candidate for Run 109
**RULE 76** could be: **META-COGNITIVE-AT-INFERENCE-TIME-PROBE** — pick axes that probe primitives for the LLM's introspective access to its own reasoning state (e.g., self-monitoring of confidence, self-correction primitives, calibration of introspective access to latent state). This widens the discovery space into the **second-order cognitive layer** of LLM behavior — primitives that operate on the model's own inference process rather than on external inputs.

Alternatively, **Rule 76 candidate (2)**: **COMPUTATIONAL-COMPLEXITY-PRIMITIVE** — pick axes that probe formal/computational-complexity primitives for LLM (decision-tree complexity of activations, circuit-depth lower bounds, sample-complexity of representation learning). Distinct from Rule 75 information-geometric which emphasizes statistical manifolds, Rule 76-COMPUTATIONAL-COMPLEXITY would emphasize algorithmic/computational lower bounds.

**Forward-discipline decision**: After Rule 75 INFORMATION-GEOMETRIC-PRIMITIVE-PROBE used in this run, the next rule should pivot to a different primitive-class entirely. Either META-COGNITIVE (introspective) or COMPUTATIONAL-COMPLEXITY (algorithmic) are candidates. META-COGNITIVE is more aligned with the cognitive-load (Rule 74) ↔ cognitive-bottleneck lineage, so likely the better choice to maintain thematic continuity while escalating to second-order primitives.
