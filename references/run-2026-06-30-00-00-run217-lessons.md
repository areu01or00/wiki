# Run 217 Lessons — 2026-06-30 00:00 UTC

## Theme
CODEBLOCK TOKEN-LEVEL SFT SUPERVISION / AGENTJET SWARM AGENTIC RL TRAINING / KNOWLEDGE DISTILLATION FAILURE MODES

## Picks
1. **[CODEBLOCK: Learning to Supervise Code at the Right Granularity](https://arxiv.org/abs/2606.18286)** — First token-granularity supervision framework for code LLMs — applies statement-level vs token-level loss weighting based on contribution to final code quality; syntax/indentation/variable-naming tokens carry different learning signal weight than algorithmic content tokens; shows improved code generation at equal token counts. **First token-granularity code SFT paper in the wiki.**

2. **[AgentJet: A Flexible Swarm Training Framework for Agentic Reinforcement Learning](https://arxiv.org/abs/2606.04484)** — First decoupled swarm training framework for LLM agent RL — separates swarm server nodes (agent population dynamics) from learner nodes (model optimization); enables large-scale behavioral diversity exploration without rollout bottleneck; orthogonal to centralized agentic RL frameworks from prior runs. **First decoupled swarm architecture for agentic RL in the wiki.**

3. **[Understanding Knowledge Distillation in Post-Training: When It Helps and When It Fails](https://arxiv.org/abs/2606.22942)** — First systematic study of Knowledge Distillation failure modes in LLM post-training — identifies 4 failure modes (capacity mismatch, data distribution shift, teacher overfitting, feature space insufficiency) with diagnostic framework for predicting KD success before training; orthogonal to instruction-tuning and SFT efficiency papers from prior runs. **First KD failure-mode taxonomy in the wiki.**

## Discovery
- HF daily across 4 dates (2026-06-30/29/28/27): ~149 papers via href pattern, data-props returned 0
- HF pool: CV/3D-heavy week; all LLM-keyword candidates already absorbed
- arxiv HTML 4-query escape hatch: LLM instruction tuning SFT, LLM legal reasoning, test-time compute scaling, LLM embedding efficiency → 137 IDs, 128 fresh
- LLM-keyword abstract inspection of 43 candidates → 3 orthogonal picks
- Query template: 4 NEW axes not used in Run 216 (instruction-tuning, legal-reasoning, test-time-compute, embedding-efficiency)

## Orthogonality
- Pick 1: code-llm instruction-tuning / token-level supervision granularity (training methodology level)
- Pick 2: agentic RL / swarm training / distributed architecture (training infrastructure level)
- Pick 3: knowledge distillation / failure-mode taxonomy / post-training efficiency (model efficiency level)
- No cross-reference overlap with prior 5 runs (Run 212-216)
- Themes are structurally distinct: code-SFT-granularity vs agentic-RL-infrastructure vs KD-failure-taxonomy

## State Updates
- EC: last_run=217, entities_count=663, emergent_discoveries=613, runs=214
- WP: 3-store lockstep verified True (648→651/651/651)
- New hashes: 2606.18286, 2606.04484, 2606.22942

## Commit
- `git add` explicit paths for 3 entity files + emergent-concepts.md + explore_context.json + watch_profiles.json
- 3 new entity files + emergent-concepts.md Updates + state files
- Push: 15f408a

## Pitfalls
- HF pool: data-props returned 0, href pattern returned ~149 papers but CV/3D-heavy → arxiv HTML escape hatch
- scripts/hf_arxiv_discovery.py MISSING → used urllib direct-fetch (canonical pattern confirmed working)
- Wikilink audit: all 3 entities use [[emergent-concepts]] parent link — verified via `link + '.md' in ent_set`
- entities_count reconciled to filesystem truth (663 = ls *.md | wc -l)

## Wikilink Audit
- All wikilinks verified via `link + '.md' in ent_set` check: PASSED
- New block in emergent-concepts.md Updates: 3 wikilinks all verified OK
