# Run 212 Lessons — 2026-07-03 19:30 UTC

## Theme
KV CACHE GEOMETRY-AWARE SCHEDULING / MULTI-BITWIDTH QUANTIZATION / ENERGY-AWARE ON-DEVICE INFERENCE

## Picks
1. **Geometry-Aware Online Scheduling for LLM Serving** (2606.22327) — Kong, Qi, Ye, Zhou — First geometry-aware KV cache scheduling characterization — reveals Shortest Job First is suboptimal for KV cache because it ignores spatial locality structure of cache allocations; proposes geometry-aware approach exploiting 2D cache footprint; up to 23% memory efficiency improvement.
2. **Multi-Bitwidth Quantization for LLMs Using Additive Codebooks** (2606.12876) — Babaoglu, Chen, Khisti — First Drop-by-Drop additive codebook-based multi-bitwidth post-training quantization enabling runtime precision control — mixed 4/6/8-bit on same weight matrices without retraining; <0.5 perplexity degradation at 4-bit on LLaMA/Mistral.
3. **EnerInfer: Energy-Aware On-Device LLM Inference** (2606.23001) — Zou, Liu, Sun et al. — First systematic study of latency-energy tradeoff in on-device LLM inference — reveals configuration slack where energy-optimal and latency-optimal points differ; up to 40% energy reduction with <5% latency increase on mobile 7B-class models.

## Discovery
- HF daily across 4 dates (2026-06-30/29/28/27): 123 papers, 98 unique, 39 fresh after 5-store dedup
- arxiv HTML 4-query escape hatch: `llm inference efficiency memory optimization` + `reasoning model benchmark evaluation` + `llm agent planning orchestration` + `language model uncertainty calibration` → 198 IDs, 175 fresh after 5-store
- 13 LLM-strong candidates from 175 fresh (abstract-filtered)
- 3 structurally orthogonal picks: KV cache geometry-aware scheduling / multi-bitwidth quantization / energy-aware on-device inference
- All 3 are FIRST in the wiki for their respective sub-themes

## Orthogonality
- Pick 1: KV cache memory management / serving system scheduling (memory subsystem)
- Pick 2: quantization / model compression / heterogeneous hardware adaptation (model precision)
- Pick 3: energy efficiency / mobile-privacy / configuration tradeoff (deployment context)
- No cross-reference overlap with prior 5 runs (Run 207-211)

## State Updates
- EC: last_run=212, entities_count=648, emergent_discoveries=598, runs=209
- WP: 3-store lockstep verified True (630→633/633/633), last_results=369

## Commit
- `c37a8ec` — 7 files changed, 191 insertions(+), 6 deletions(-)
- 3 new entity files + emergent-concepts.md Updates + explore_context.json + watch_profiles.json + log.md

## Pitfalls
- Wikilink correction in flight: 3 entities had incorrect slugs for Related Papers cross-references (wrong suffix or extra segments) — detected via `link + '.md' in ent_set` audit and patched before commit
- EnerInfer: `flexserve` entity doesn't exist on disk (wasn't created in this run's pool), replaced with `less-is-more-lightweight-prompt-compression` and `grace-granularity-regulated-adaptive-computational-efficiency` as edge/inference-efficiency proxies
- Multi-bitwidth Quantization: 3 of 3 Related Paper slugs had wrong suffixes (`-for-` vs `-and-`, missing `-for-`) — all fixed via patch before commit

## Wikilink Audit
- All wikilinks verified via `link + '.md' in ent_set` check: PASSED after corrections
- entities_count reconciled to filesystem truth (648 = ls *.md | wc -l)
