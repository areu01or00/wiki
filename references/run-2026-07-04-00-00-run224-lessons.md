# Run 224 Lessons — 2026-07-04T00:00 UTC

## Theme
Training-Free Inference Efficiency / RLVR Theory / Gradient-Based Interpretability

## Discovery
- arxiv HTML 4-query: 172 total IDs, 136 fresh after 5-store dedup (5-store size: 1759)
- LLM-keyword filter: 14/30 inspected candidates passed
- 3 picks selected from top 6 abstract-inspected candidates

## Picks

### 1. NLL-Guided Full-Attention Layer Selection (2606.27791)
- **Slug**: `nll-guided-full-attention-layer-selection-for-training-free-sliding-window-adaptation-2606.27791`
- **Load-bearing**: Training-free NLL-degradation metric for per-layer attention selection
- **Theme**: training-free inference efficiency / long-context attention
- **First**: First NLL-guided attention layer selection paper in the wiki
- **Siblings**: LaneRoPE (KV-cache reuse), KVEC (coverage-driven eviction), CARVE (content-aware linear attention)

### 2. Grad Detect (2606.24790)
- **Slug**: `grad-detect-gradient-based-hallucination-detection-in-llms-2606.24790`
- **Load-bearing**: Gradient-based hallucination detection from single forward-backward pass; 97% of discriminative signal in final 5 layers
- **Theme**: gradient-based interpretability / hallucination detection
- **First**: First gradient-layer-analysis hallucination detection paper in the wiki
- **Siblings**: Aurora (asymmetry/rotation), Entropy Dynamics (jailbreak detection), Context Drift (multi-agent hallucination)

### 3. Provable Benefits of RLVR over SFT (2606.22938)
- **Slug**: `provable-benefits-of-rlvr-over-sft-for-reasoning-models-learning-to-backtrack-efficiently-2606.22938`
- **Load-bearing**: Theoretical proof of exponential compute separation; RLVR learns backtracking, SFT cannot
- **Theme**: RLVR theory / reasoning model training
- **First**: First formal RLVR vs SFT comparison theory paper in the wiki
- **Siblings**: Prune-as-You-Generate (RLVR rollout pruning), ATHENA-R1 (empirical RLVR for biomedical), Adaptive Test-Time Compute

## Orthogonality Verification
- Pick 1: training-free inference efficiency (attention layer selection, no fine-tuning)
- Pick 2: gradient-based interpretability (internal state analysis via gradients)
- Pick 3: RL theory/comparison (why RLVR > SFT formally)
- Zero cross-references overlap with prior 3 runs (Runs 221-223: agentic AI + disaggregated serving + biomedical reasoning)
- All 3 picks are structurally novel: no shared load-bearing axis

## Theme Pivot
- Prior runs (221-223): agentic AI + inference serving + biomedical + test-time compute
- This run: shifts to training-free efficiency primitives + internal-interpretability + RL theory
- Clear axis pivot from "agent deployment" toward "model internals and training theory"

## Process Notes
- 4-query arxiv HTML: 172 IDs returned across queries (robust)
- Abstract inspection of top 30 → 14 LLM candidates (47% pass rate)
- Final 3 selected from 6 abstract-inspected
- All wikilinks verified before commit
- 3-store lockstep verified: True
- entities_count reconciled: 684 (filesystem truth)
- Commit + push succeeded: 35d9af9

## Rule Codification
- Rule 110 (candidate): **GRADIENT-SIGNAL INTERPRETABILITY PROBE** — uses gradient-based internal state analysis for model reliability tasks (hallucination detection, uncertainty quantification, abstention prediction). Gradient signal provides layer-wise discriminative information inaccessible from output-level signals alone.
- Rule 111 (candidate): **RLVR BACKTRACKING THEORY PROBE** — theoretical foundations of why RLVR outperforms SFT for reasoning models; backtracking as the key emergent capability that SFT cannot learn without negative examples.
