# Run 126 Lessons — CAUSAL-REPRESENTATION-PROBE (Rule 83)

## Theme: CAUSAL-REPRESENTATION-PROBE
Rule 83 CAUSAL-REPRESENTATION-PROBE: how LLM internal representations encode causal structure — causal representation learning, causal disentanglement in transformer activations, causal concept discovery, causal geometry of representations.

## Discovery Narrative

### Query Strategy
Direct arxiv HTML search (6 queries × 50 results = ~300 candidates) via `urllib.request`:
1. `LLM representation learning causal structure` → 48 results
2. `LLM internal representations causal discovery` → 5 results
3. `mechanistic interpretability causal LLM circuits` → 17 results
4. `LLM causal representation geometry 2026` → 14 results
5. `sparse autoencoder causal features LLM` → 45 results
6. `LLM representation causal intervention reasoning` → 21 results
Total: 132 unique candidates (after dedup across queries)

### Dedup
5-store dedup against: entity filenames + `ec['emergent_concept_papers']` + `ec['chains']['emergent-concepts']['papers_found']` + `wp['last_result_hashes']` (3 stores) + `wp['profiles']['llm-wiki-explore']['last_results']`.
- Entity files: 384 on disk
- After 5-store dedup: 20 fresh out of 21 arxiv-listed candidates (one was already in chain papers_found)

### Picks (3 structurally orthogonal)
All 3 share "causal reasoning about LLM representations" theme but orthogonal load-bearing axes:

1. **Belief-or-Circuitry (2605.08405)** — load-bearing = **hybrid causal/statistical mechanism decomposition** for in-context learning via graph random-walk causal interventions. Axes: causal-vs-statistical-in-context decomposition + graph-topology-tracking mechanism + mechanism verification via intervention. Orthogonal to the other two: it's mechanism-level (not framework-level like Causality-is-Key, not depth-level like Emergent-Causal-Geometric).

2. **Causality-is-Key (2602.16698)** — load-bearing = **causal validity criterion for interpretability** — specifies what causally valid mappings from activations to high-level structures mean. Axes: causal-fidelity-criterion + interpretability-failure-mode-taxonomy + methodology foundation. Orthogonal to the other two: it's framework/methodology level (not mechanism-level like Belief-or-Circuitry, not depth-level like Emergent-Causal-Geometric).

3. **Emergent-Causal-Geometric (2602.04931)** — load-bearing = **depth-dependent causal intervention efficacy** linking geometric structure to causal dynamics across transformer layers. Axes: depth-causality-profile + causal-geometric-dynamics + layer-wise-representation-structure. Orthogonal to the other two: it's depth-geometry level (not mechanism-level like Belief-or-Circuitry, not framework-level like Causality-is-Key).

## Pitfalls
No new pitfalls this run. All wikilinks verified against `os.listdir('entities/')` ground truth. No broken slugs, no self-references, no extra `.md` suffixes.

### entities_count reconciliation
`entities_count` reconciled to filesystem truth via `len([f for f in os.listdir('entities/') if f.endswith('.md')])` = 387.

## State Changes
- **watch_profiles.json**: 3 hash stores updated from 369 → 372 via union+sort (3-store lockstep)
- **explore_context.json**: 3 `emergent_concept_papers` (363 total); 3 `emergent_discoveries` (367 total); 3 `papers_found` in chains['emergent-concepts']; 1 `runs` entry (123 total); 1 `emergent_concept_search_runs` entry; 1 `emergent_concept_search_log` entry
- **parent emergent-concepts.md**: new block prepended after position 921
- **3 new entity files**: Belief-or-Circuitry, Causality-is-Key, Emergent-Causal-Geometric

## Result
- **Commit**: `a4252fd`
- **Run 127 candidate**: Rule 84 ADVERSARIAL-REPRESENTATION-PROBE — probing adversarial attack surfaces in LLM representations (representation poisoning, Trojan latent triggers, stealth backdoors, feature-level adversarial perturbations)
