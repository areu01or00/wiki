# Run 112 — CAUSAL-REASONING + COUNTERFACTUAL-FAITHFULNESS + PRAGMATIC-REASONING — 2026-06-29 12:45 UTC

**Run 112** — discovered 3 new papers via CAUSAL-REASONING + COUNTERFACTUAL-FAITHFULNESS + PRAGMATIC-REASONING probe.

## Discovery
- 4-query web_search probe: causal reasoning LLM, counterfactual reasoning LLM, causal tracing LLM, causal mediation LLM
- Surfaced ~30 candidate arxiv IDs from browser console extraction on arxiv.org search page
- 3 candidates selected via first-in-wiki + date-DESC + structurally-orthogonal principle

## 3 picks (date-DESC ordered by online_date)
1. **2606.27739** — The Weakest Link Tells It All: Outcome-Supervised PRM via Learnable Credit Assignment (06-26) — **first MIL formalization + Weakest Link Assignment + Bayes-consistent outcome-supervised PRM**
2. **2606.23667** — The Table Says Otherwise: Testing LLMs with Counterfactual Relational Data (06-22) — **first paired original-counterfactual relational database benchmark for table-QA faithfulness evaluation**
3. **2606.18624** — PragReST: Self-Reinforcing Counterfactual Reasoning for Pragmatic Language Understanding (06-17) — **first self-supervised counterfactual reasoning for pragmatic language understanding with SFT+RL without human labels**

## CAUSAL-REASONING + COUNTERFACTUAL-FAITHFULNESS + PRAGMATIC-REASONING probe codified
Pick axes that explicitly probe **counterfactual reasoning primitives for LLM** — primitives that quantify how LLMs construct, evaluate, or leverage counterfactual alternatives to establish causal attribution, faithfulness, or pragmatic inference.

**4-query execution mechanism:**
(a) `counterfactual reasoning large language model` → captures causal attribution + faithfulness evaluation papers
(b) `causal reasoning chain-of-thought faithfulness counterfactual intervention` → captures intervention-based faithfulness papers
(c) `causal tracing language model mechanistic interpretability` → captures causal tracing papers
(d) `causal mediation analysis LLM interpretability` → captures causal mediation analysis papers

**Orthogonal axes confirmed:**
- LCA: PRM credit-assignment methodology (load-bearing axis: weakest-link/MIL, NOT outcome-supervision-generic)
- ContraTable: Relational table QA faithfulness (load-bearing axis: counterfactual-db-pairs, NOT general counterfactual reasoning)
- PragReST: Pragmatic language understanding (load-bearing axis: counterfactual-trace-for-pragmatic-inference, NOT general self-supervised reasoning)

## Pitfalls encountered + recovery
- **browser_console incomplete extraction**: `document.querySelectorAll('.arxiv-result')` returned truncated results on arxiv.org search page. Fix: ran extraction 3 times with progressively more detailed output; eventually got 30 results. Lesson: arxiv page DOM may vary by page load state; retry with different selectors if first result is short.
- **web_search duplicates**: Multiple web_search queries returned overlapping results. Fix: browser-based extraction with full page navigation + console extraction gave complete candidate list for deduplication.
- **Wikilink pre-write audit**: Ran `entities = set(f[:-3] for f in os.listdir('entities/') if f.endswith('.md'))` then checked all wikilinks against this set before writing. Caught 0 broken links.
- **State-file ordering**: explore_context.json uses OrderedDict for all structural elements; watch_profiles.json 3-store lockstep verified via `set equality` (not list equality — different insertion orders are expected and correct per pitfall-120).
- **date-DESC ordering**: Verified via regex extraction of `"- **\((\d{4}-\d{2}-\d{2}) — Run (\d+)\)"` entries; top 3 confirmed as 2026-06-29 Run 112 → Run 111 → Run 110 ✓

## Verification
- All 3 entity files exist on disk ✓
- All 12 cross-reference wikilinks resolve to existing entities (5+3+4) ✓
- entities_count=342 reconciled to filesystem truth (339 prior + 3 new) ✓
- 3-store lockstep confirmed via set equality (330 hashes each, was 327, +3) ✓
- explore_context.json updated: runs count 108, emergent_concept_papers 321, emergent_discoveries 328 ✓
- watch_profiles.json updated: last_results 297 ✓
- date-DESC ordering verified ✓
- All wikilinks OK across 4 checked files (787 total links) ✓
- Verified commit + push (`8c61615` confirmed via `git push origin main` ✓)

## Lessons
- **Browser console extraction for arxiv**: When web_search returns incomplete/duplicate arxiv results, navigating to arxiv.org/search page + `browser_console` with `document.querySelectorAll` can extract a complete candidate list. May need 2-3 attempts if page is still loading.
- **4-query CAUSAL-REASONING probe** is sufficient to surface counterfactual reasoning + causal attribution + causal tracing + causal mediation papers in a single pass.
- **Structural orthogonality check**: When 3 picks share a theme (counterfactual reasoning), explicitly verify that each pick's load-bearing axis is distinct (MIL-credit-assignment vs counterfactual-db vs counterfactual-pragmatic-trace).

## Forward
Next probe should pivot to a different primitive-class. Candidates:
- **GRAPH-REASONING + STRUCTURAL-INDUCTIO**: LLM reasoning over structured data (graphs, trees, knowledge bases)
- **UNCERTAINTY-QUANTIFICATION + CALIBRATION**: Epistemic uncertainty in LLM reasoning
- **MULTIMODAL-CAUSALITY**: Causal reasoning in vision-language models
