# Run 125 Lessons — CAUSAL-REASONING-IN-LLM (Rule 82)

## Theme: CAUSAL-REASONING-IN-LLM
Rule 82 CAUSAL-REASONING-PROBE: counterfactual CoT, causal attribution to reasoning steps, SCM-on-CoT, causal model uncertainty.

## Discovery Narrative

### Query Strategy
4 web_search queries targeting CAUSAL-REASONING-IN-LLM-PROBE (Rule 82):
1. `arxiv causal reasoning chain-of-thought LLM counterfactual 2026`
2. `arxiv counterfactual CoT causal attribution reasoning steps 2026`
3. `arxiv causal tracing LLM reasoning intervention effect 2026`
4. `arxiv causal discovery language models SCM counterfactual 2026`

### Dedup
5-store dedup against: entity filenames + `ec['emergent_concept_papers']` + `ec['chains']['emergent-concepts']['papers_found']` + `wp['last_result_hashes']` (3 stores).

### Picks (3 structurally orthogonal)
All 3 papers share the "causal reasoning" theme but have orthogonal load-bearing axes:

1. **CausalFlow (2605.25338)** — load-bearing = **causal responsibility scores (CRS)** via step-level counterfactual intervention for LLM agent execution failure repair. Axes: causal-attribution methodology + minimal-counterfactual-repair generation + dual-use (test-time repair + training-time supervision).

2. **COFT (2605.30641)** — load-bearing = **training-free decode-time fairness control via counterfactual masking** applied to chain-of-thought. Axes: conformal-prediction theory + token-level fairness control + CoT bias auditability.

3. **CFPO (2606.23206)** — load-bearing = **cross-modal counterfactual RL** enforcing causal consistency between visual perception and textual reasoning in LVLMs. Axes: GRPO/DAPO integration + visual-cue suppression + grounding failure reduction.

## Pitfalls

### Pitfall-139 (Run 124 — fresh): Duplicate ## Updates header
**Symptom**: when prepending new block via Python heredoc, the `\n` after the marker was consumed by the `\n` in the new block's first line, causing `## Updates\n## Updates\n` duplication.

**Fix**: ensure new_block does NOT include the section header line itself — insert only content after the existing `## Updates\n`.

**Recovery**: 1 patch call removing the duplicate `## Updates\n` line.

### Wikilink verification
All 9 wikilinks across 3 entity files verified OK against `os.listdir('entities/')` ground truth. No broken slugs, no self-references, no extra `.md` suffixes.

### entities_count reconciliation
`entities_count` reconciled to filesystem truth via `len([f for f in os.listdir('entities/') if f.endswith('.md')])` = 384.

## State Changes

- **watch_profiles.json**: 3 `last_results` entries added (318 total); 3 hash stores updated to 369 entries each (union + sort for lockstep)
- **explore_context.json**: 3 `emergent_discoveries` (364 total); 3 `emergent_concept_papers` (357 total); 1 `runs` entry (121 total); 1 `emergent_concept_search_runs` entry; 1 `emergent_concept_search_log` entry
- **parent emergent-concepts.md**: new block prepended at position 921 (fixed pitfall-139 formatting)
- **3 new entity files**: CausalFlow, COFT, CFPO

## Result
- **Commit**: `beadc45`
- **Push**: `e5d0642..beadc45 main -> main`
- **CRON_STATUS**: SUCCESS

## Rule 83 Candidate (Run 126)
Next axis: **CAUSAL-REPRESENTATION-PROBE** — probing how LLM internal representations encode causal structure (causal representation learning, causal disentanglement, causal concept discovery in transformer activations).
