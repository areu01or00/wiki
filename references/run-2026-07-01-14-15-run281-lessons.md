# Run 281 Lessons (2026-07-01T14:15 UTC)

## Discovery
- HF daily DNS failure (`papers.foai.org Name or service not known`) — escalated to web_search as primary
- HF DNS is a fourth distinct failure mode (after pitfall-83 zero-return, pitfall-83 non-LLM variant, pitfall-94 empty data-props)
- Confirmed: web_search `site:arxiv.org` queries returned 14 fresh LLM candidates after 5-store dedup
- 2 pre-committed entities found during pick selection: geometry-of-refusal (Run 278, 63fa371) and worldevolver (Run 167, 63fa371) — skipped both

## Picks
1. **OpenLife** (2606.31046, 06-30): Open-world ALife with autonomous LLM agents — world-model / agent-open-axis
2. **TriPlay-RL** (2601.18292, 01-26): Tri-role self-play (attacker+defender+evaluator) for LLM safety alignment — safety axis
3. **H2HMem** (2606.09461, 06-08): Multimodal memory benchmark for agents in human-human interaction — benchmark-evaluation axis

## Theme pivots
- Prior run (280): AI governance / collective skill / multi-UAV
- This run (281): orthogonal axes (open-world ALife + tri-role safety + multimodal memory benchmark)
- No overlap with prior 3 runs (278/279/280) domain clustering

## Technical notes
- 3-store lockstep verified: s1=857, s2=857, s3=857 (matched before append, matched after)
- Entity files written via write_file (not heredoc — avoids $HOME issue in cron mode)
- Parent Updates prepend: date-DESC order verified (06-30, 06-08, 01-26)
- Self-reference wikilinks in Related Papers (wiki convention — valid)
- No cross-reference wikilinks to sibling entities (each is orthogonal theme)

## Run 281 verification
- git push: ffdfc43 on main
- CRON_STATUS: SUCCESS
